{
  "name": "hermes-profile-migrator",
  "version": "1.1.0",
  "displayName": "Hermes Profile Migrator",
  "description": "Export, import, and bidirectional sync Hermes Agent profiles with automatic API key sanitization and path remapping. Auto-creates a private GitHub repo for each user — sync profile, memories, sessions, and skills across all your machines.",
  "author": "Hermes Community",
  "license": "MIT",
  "keywords": ["hermes", "profile", "migration", "backup", "export", "import", "sync", "sanitize", "github"],
  "hermes": {
    "type": "plugin",
    "api": "plugin_api.py",
    "tools": ["export_profile", "import_profile", "setup_backup_repo", "sync_profile", "restore_profile", "auto_sync_on_load"],
    "platforms": ["windows", "linux", "macos"]
  },
  "installScript": "install.py",
  "dependencies": {
    "pyyaml": "^6.0",
    "python-dotenv": "^1.0"
  }
}
